#pragma once

using namespace std;

class Circle{
    public:
    double area(int radius);
    double per(int radius);
};

class Rectangle{
    public:
    double area(int l, int t, int r, int b);
    double per(int l, int t, int r, int b);
};